name: Android APK Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          libffi-dev \
          libssl-dev \
          libsqlite3-dev \
          zlib1g-dev \
          python3-pip \
          autoconf \
          automake \
          libtool \
          pkg-config \
          cmake \
          gettext \
          libltdl-dev \
          zip \
          unzip
        pip install --upgrade pip
        pip install buildozer cython==0.29.33

    - name: Build with Buildozer
      run: |
        # Buildozer'ın NDK ve SDK'yı GitHub üzerinde otomatik kurması için
        yes | buildozer android debug
      env:
        TERM: xterm

    -     - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: KuryeTakip-APK
        path: bin/*.apk
        if-no-files-found: error 
        overwrite: true 
